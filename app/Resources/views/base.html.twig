<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Listas abiertas - Red Ciudadana Partido X{% endblock %}</title>
    <link href='//fonts.googleapis.com/css?family=Open+Sans' rel='stylesheet' type='text/css'>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.1.1/css/bootstrap.min.css">
    <link href="{{ asset('bundles/rcpx/css/bootstrap-votolab.css') }}" rel="stylesheet" media="screen">
    <script src="{{ asset('bundles/fosjsrouting/js/router.js') }}"></script>
    <script src="{{ path('fos_js_routing_js', {"callback": "fos.Router.setData"}) }}"></script>
    <link rel="icon" type="image/x-icon" href="{{ asset('bundles/rcpx/images/favicon.ico') }}"/>
    <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
    <script src="//oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
    <script src="//oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
    <![endif]-->
    {% block viewStylesheets %} {% endblock %}
</head>
<body>
{% block navbar %}
    <nav class="navbar navbar-default navbar-static-top" role="navigation">
        <div class="container">
            <ul class="nav navbar-nav navbar-collapse">
                <a class="navbar-brand" href="{{ path('rcpx_homepage') }}">
                    <img src="{{ asset('bundles/rcpx/images/partidox-redciudadana.png') }}" class="img-responsive"/>
                </a>
            </ul>
            <ul class="nav navbar-right navbar-nav">
                {% if is_granted('ROLE_ADMIN') %}
                    <li>
                        <a type="button" class="btn navbar-btn " type="button" href="{{ path('votolab_admin') }}">
                            <span class="glyphicon glyphicon-plus"></span> {{ 'admin_panel'|trans|capitalize }}
                        </a>
                    </li>
                {% endif %}
                <li>
                    <a type="button" class="btn navbar-btn " type="button" href="{{ path('fos_user_change_password') }}">
                        <span class="glyphicon glyphicon-cog"></span> {{ 'change_password'|trans|capitalize }}
                    </a>
                </li>
                <li>
                    <a type="button" class="btn btn-default navbar-btn" type="button"
                       href="{{ path('fos_user_security_logout') }}">
                        <span class="glyphicon glyphicon-log-out"></span> {{ 'logout'|trans|capitalize }}
                    </a>
                </li>
            </ul>
        </div>
    </nav>
{% endblock %}

<div class="container">
    {% block body %}{% endblock %}
    <div class="footer">Red Ciudadana - Partido X</div>
</div>
<!-- /container -->
<script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
<script src="//netdna.bootstrapcdn.com/bootstrap/3.1.1/js/bootstrap.min.js"></script>
{% block viewScripts %} {% endblock %}
</body>
</html>